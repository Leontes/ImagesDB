language: python
python:
 - "2.7"
# command to install dependencies
install:
 - sudo apt-get install make
 - make install
# command to run tests
script:
 - make run_tests

branches:
  - only:
    - master
